{
  "name": "amocrm_test",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.js",
  "type": "module",
  "scripts": {
    "start": "parcel src/index.pug --open",
    "build": "parcel build src/index.pug -d dist --public-url ./",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.1.0",
    "pug": "^3.0.2"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
